# Reeve Bot Configuration

# Path to user's Desk repository (your personal context)
# This is where Reeve's context files live (CLAUDE.md, Goals/, etc.)
REEVE_DESK_PATH=~/my_reeve

# Database Configuration
# For sync operations (Alembic migrations): sqlite:///~/.reeve/pulse_queue.db
# For async operations (daemon): sqlite+aiosqlite:///~/.reeve/pulse_queue.db
PULSE_DB_URL=sqlite+aiosqlite:///~/.reeve/pulse_queue.db

# HTTP API Configuration
PULSE_API_PORT=8765
PULSE_API_TOKEN=your_secret_token_here

# Hapi Configuration
# Path to the Hapi executable (usually just "hapi" if in PATH)
HAPI_COMMAND=hapi

# Telegram Bot Configuration (for Telegram integration)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=~/.reeve/logs/daemon.log

# Extra writable directories for systemd sandbox (space-separated absolute paths)
# The daemon runs with ProtectHome=read-only. Core paths (~/.reeve, ~/.cache/uv,
# ~/.hapi, ~/.claude, and REEVE_DESK_PATH) are always writable.
# Add paths here for MCP servers that need writable storage.
# Example: REEVE_EXTRA_RW_PATHS=/home/user/.google_workspace_mcp /home/user/.some_other_mcp
REEVE_EXTRA_RW_PATHS=
